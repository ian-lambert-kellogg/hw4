<h1><%= @place["name"] %></h1>

<p>
<a class="btn btn-primary" href="/entries/new?place_id=<%= @place["id"] %>" role="button">New Post</a>
</p>

<% if session["user_id"] != nil    %>
<h2>My Entries</h2>
<% for entry in @entries %>
  <h3><%= entry["title"] %></h3>
  <h4><%= entry["occurred_on"] %></h4>
  <p><%= entry["description"] %></p>
<% end %>
<% else %>

Login to see your entries

<% end %>



